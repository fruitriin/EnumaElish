# Hook sections test
preToolUse
  allow find
    next: bulkExec
  deny rm
    mode: block
    message: "Use trash instead"

postToolUse
  allow WebFetch
    mode: hint
    message: "結果をファイルに書き出してください"

template bulkExec
  |,>>
    deny rm  "don't pipe into destructive"
