# Basic rules test
allow find
  |,>>
    allow touch, cat
  |,>>
    deny rm  "don't combine find with rm"
  exec:
    deny rm  "expand to tempfile first"
    allow cp, mv, touch

allow grep
  |,>>
    allow wc, sort, head, tail

deny rm   # top-level rm is deny
