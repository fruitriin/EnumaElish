# 進捗表

## 運用ルール

### タスク開始時
1. `.claude/addf/Feedback.md` を読み、前回の改善アクションで未対応のものがあれば考慮する
2. 以下の手順で Markdown チェックリストを作成する
   1. 1ショットで作業できる範囲にサブタスクを分割する
   2. 並行作業できる粒度でさらに分割する
   3. 各サブタスクにテスト作成・統合テスト・Lint・ビルドが必要か検討し、必要なら追加する
   4. 必要に応じて 2.1〜2.3 を再帰的に適用する

### 作業中
3. サブタスク着手時に `- [x]` でチェックしていく。並列可能なタスクはコンテナオーケストレーションを利用する
   - Plan の曖昧さで確信が持てないときは CLAUDE.md「迷ったときの作法（7割共有原則）」に従う（閾値割れなら `.claude/addf/Questions.md` に質問を置いてタスクを切り替える）
   - 長大なタスクでは、サブタスク完了時点でブランチ `checkpoint/<phase>-<N>` を切ってよい。別方針を試すときは checkpoint から `alt/` を分岐する
3.5. **日記を書く（代替わり引き継ぎ）**（「3.5」は後続の番号参照を壊さないための意図的な枝番）: resume・compaction・`/loop` の次イテレーションで起きる「小さな代替わり」のたびに、次の代の自分（同僚でもあり、寝て起きたあとの自分でもある）が状況に入れるよう、タスクの「#### 日記」セクションにエントリーを書く
   - **書くタイミング**: サブタスク完了時 / 重要な判断をした直後 / 計画を変更したとき / コンテキストが長くなり compaction を予感したとき
   - **書式（4項目）**（時刻 HH:MM は省略可）:
     ```
     ##### YYYY-MM-DD HH:MM — <出来事の一行>
     **やったこと**: <完了した作業と判断の要約>
     **今の見立て**: <現状認識。確信度があれば記す>
     **次の自分へ**: <次に着手すべきこと・先に確認すべきこと>
     **気になっていること**: <未解決の不確実性・前提・違和感。なければ「なし」>
     ```
   - 「日記」という語彙の意図（「遺書」を使わない理由）は `.claude/addf/guides/development-process.md` 参照
   - ブランチ checkpoint が「何がコミットされたか（事実）」を残すのに対し、日記は「なぜそうしたか・次に何を考えていたか（文脈）」を残す。両方で前任者の靴に履き替えられる
   - 日記の自動生成フックは導入しない。書くこと自体が思考の整理であり、次の自分への手紙として人格を持って書く
4. 実装フェーズの最終サブタスク完了時、以下の知見を `/addf-knowhow` で記録する（既存 knowhow の更新も含む）:
   - **コーディング知見**: 実装中に発見した再利用可能なパターン、落とし穴、技術的判断とその根拠
   - **分かれ道の目印**: 差し戻し・やり直し・想定外の判断が発生したサブタスクがあれば、使用したスキルの `.exp.md`「🔀 分かれ道の目印」にも追記する（書式: `.claude/addf/templates/ExperienceTemplate.md`。失敗の告白ではなく、意思決定が枝分かれしたポイントと次に同じ分岐に立ったときの選び方を道標として書く）

### エージェント起動時の共通ルール
- エージェントチーム（TeamCreate）やサブエージェント（Agent）を作成するとき、各エージェントへのプロンプトに **最初に `/addf-knowhow-index` を実行する** よう指示を含めること
- これにより各エージェントがプロジェクトの知見ベースを把握した状態で作業を開始できる

### タスク完了時 — 品質検証

4. プロジェクトのビルド・Lint・テストコマンドを実行する
   - ADD フレームワークテスト: `bash .claude/addf/tests/run-all.sh`
   - **失敗した場合 → 実装に差し戻す**。原因分析 → 修正 → 再実行
5. `addf-code-review-agent` でコードレビューを実施する
   - 通常タスクは単体（ペルソナなし）で起動する
   - **マイルストーン・リリース直前・`mode: critical` 宣言時・unattended 自走時（`/addf-mode unattended`）**は、ペルソナ並列（視点ずらしレビュー）を起動する。起動前に `.claude/agents/addf-code-review-agent.md` を読み、ペルソナ定義に従うこと
   - ペルソナ並列の集約: 同一箇所・同一原因の指摘は1件にまとめてペルソナを列挙する。**2ペルソナ以上が独立に指摘した項目は重要度を1段上げる**（コンセンサス補正）
6. `addf-contribution-agent` で ADD フレームワークへのコントリビューション候補を検出する
7. レビュー指摘への対応:
   - **Critical/High**: 必ずこのフェーズ内で修正する（先送り禁止）
   - **Medium**: 原則修正。先送りする場合は独立計画を起こす
   - **Low/Info**: Plan に記録し、必要に応じて独立計画で対応
   - **バグ分離**: 発見されたバグが現在のプランと関心事が異なる場合は、修正せずに新しいプラン（`.claude/addf/plans/`）を書き起こし、`TODO.md` に追加するのみで現在のプランを完了させる
   - 修正後、ビルド・Lint・テストを再実行して通過を確認する
8. 品質ゲートで得た知見を `/addf-knowhow` で記録する:
   - **品質ゲート知見**: レビューエージェントが検出したパターン（セキュリティ、コード品質、分離パターン違反等）のうち、他のタスクでも再発しうるもの

#### ノウハウ蓄積

9. 投入されたタスクのPlanに実装完了状況を反映する
10. タスク全体の総括知見を `/addf-knowhow` で記録する:
    - **タスク総括**: 計画と実装のギャップ、想定外だった点、次回同種タスクへの教訓。コーディング・品質ゲートで既に記録した知見と重複しないこと

#### フィードバック記録

11. `.claude/addf/Feedback.md` にPlan, TODO, Progress推進エンジンの問題の記録・改善アクションを追記する。反映済みの項目は削除する
12. `.claude/addf/Feedback.md` にプロジェクト進行上の問題の記録・改善アクションを追記する。反映済みの項目は削除する
13. Progress 推進エンジン自体に関するフィードバック・ノウハウがあれば、テンプレート（`.claude/addf/templates/ProgressTemplate.addf.md`）の改善案を `.claude/addf/Feedback.md` に記録する

#### アーカイブとコミット

14. `.claude/addf/Progresses/YYYY-MM-DD-プラン名.md` にリネームして移動し、`.claude/addf/templates/ProgressTemplate.addf.md` から新規の Progress.md を作成する
15. コミットする

---

## タスク

### 現在のタスク: Plan 0028 フェーズ3-4 — guides 追記と /addf-overview 再生成

出典: `.claude/addf/plans-add/0028-worktree-speculative-dev.md` フェーズ3-4（最終フェーズ）。副次効果: Plan 0029 残課題 L1（project-overview の GUI 常設前提）も再生成で解消。

#### サブタスクチェックリスト

- [ ] .claude/addf/guides/ に投機運用ガイド追記（speculative 2層モデル・enable オプトイン・clean・昇格の概観。詳細はスキル本文参照の構成）
- [ ] /addf-overview 再生成（.claude/commands/addf-overview.md の手順に従う。L1 解消の確認込み）
- [ ] Stage 1: run-all 全パス＋lint 一式
- [ ] レビュー（単体 — 生成物中心の差分のため。guides 追記と生成手順の妥当性に絞る。ペルソナ並列にしない判断の理由: 機械的再生成の巨大 diff に3体は費用対効果が合わない）
- [ ] Plan 0028 完了化・0029 L1 解消記録・TODO 更新 → アーカイブ → コミット（push しない）

#### 日記

##### 2026-07-04 — /loop サイクル5 開始（cron 4連発火は重複として1回分実行）
**やったこと**: タスク選択（0028 3-4 で Plan 0028 完全完了へ）。
**今の見立て**: ドキュメント生成中心の低リスクタスク。overview 再生成は addf-overview.exp.md の経験を委譲エージェントに読ませること。
**次の自分へ**: **push 保留4コミット（838f172・4f1afe7・02df655・ed7d06c）、Issue #18/#19/#20 返信、v0.5.0 リリース判断 — すべてオーナー確認待ち**。/loop cron 毎時:23（ID 2dc5f16d）。
**気になっていること**: cron がアイドル中にキューされ4連発火した。次サイクルも同様なら「未処理の同一プロンプトはまとめて1回」と扱う（今回の前例）。

##### 2026-07-04 — 【中断】使用クレジット枯渇によりループ停止
**やったこと**: サイクル5（0028 フェーズ3-4）の委譲エージェントが「out of usage credits」で即死（作業未着手・変更なし）。cron ジョブを削除してループを停止。
**今の見立て**: 本タスク（0028 3-4: guides 追記＋overview 再生成）は**未着手のまま**。作業ツリーはクリーン（コミット済み ed7d06c まで）。
**次の自分へ（クレジット補充後の再開手順）**: (1) このタスクをチェックリスト先頭から再開（委譲プロンプトは日記直前のサイクル5開始エントリ参照）(2) ループ再開は `/loop 1h [自律行動1時間] addf-speculate もしくは addf-dev を適宜実行` を再実行 (3) **オーナー確認待ち: push 保留4コミット（838f172・4f1afe7・02df655・ed7d06c）/ Issue #18/#19/#20 返信 / v0.5.0 リリース判断**。
**気になっていること**: なし（状態はすべてコミット済み＋この日記に保存）。

##### 2026-07-05 — オーナー指示で push 保留4コミットを PR #21 として提出
**やったこと**: オーナーの「現状を確認 投機ブランチをPR」指示に対応。投機ブランチ2本（speculative/test-scenario-optin-note・lint-uv-note）は 7/3 に昇格済み（12cc5d0・5e42691 として origin/main 反映済み、ブランチは clean で削除済み）と確認。オーナー確認待ちだった push 保留4コミット（838f172・4f1afe7・02df655・ed7d06c）を `plan-0033-0028-0034-batch` ブランチとして push し、PR #21 を作成した。
**今の見立て**: 「オーナー確認待ち」3件のうち push 保留分は PR レビューという形で解消へ。ローカル main は origin/main より4コミット先行のまま（PR マージ後に整合する）。
**次の自分へ**: PR #21 マージ後、ローカル main と origin/main の整合を確認すること（squash マージだった場合はローカル main の付け替えが必要）。残るオーナー確認待ち: Issue #18/#19/#20 返信 / v0.5.0 リリース判断。タスク 0028 フェーズ3-4（guides 追記・overview 再生成）は未着手のまま。
**気になっていること**: PR #21 のマージ方式（squash/merge）でローカル main の後処理が変わる。

##### 2026-07-05 — オーナーとの設計対話で Plan 0035〜0037 を起案（実装は未着手）
**やったこと**: PR #21 のレビュー体験から連鎖したオーナーとの対話で3計画を起案。0035（PR 本文 Plan リンク標準・昇格 PR 経路・部分昇格＋Pending・誤完了防止3点セット）、0036（/addf-plan-audit で過去の埋没タスク回収・migrate ワンショット統合）、0037（docs 明け渡し・.claude/addf/ 集約・paths.toml 単一ソース化）。0033⇔0035⇔0036⇔0037 を相互リンクで接続（0035 項目3規約の自己適用）。
**今の見立て**: 実装順は 0035 → 0036 → 0037 を推奨（0037 は差分巨大・メジャー級のため最後。着手中は投機停止）。0035 着手時に 0028 フェーズ3-4（guides）と 0033 項目4（PlanTemplate）を引き取るか決める。
**次の自分へ**: オーナーは対話に乗り気で全提案に好意的。0035 実装から始めるのが自然。PR #21 は open のまま（マージ後にローカル main の整合確認を忘れない）。push 保留がさらに積み上がっている（PR #21 の4＋計画6コミット）。
**気になっていること**: 計画6コミットは PR #21 に含まれない（PR ブランチは push 時点のスナップショット）。オーナーが PR #21 をマージした後、計画コミット分をどう出すか（第2 PR か、直接 push 許可をもらうか）確認が必要。

##### 2026-07-05 — PR #21 マージ完了（プロンプト指示承認）・投機上限 7 へ
**やったこと**: オーナーが「33 よさそう・28 よさそう・34 よさそう」とレビュー承認（0035 で決めた「プロンプト指示で行う」チャネルの初適用）。head コミット ed7d06c を `git push origin ed7d06c:main` で直接 push し PR #21 を MERGED に（マージコミットを作らず履歴一直線・ローカル main との分岐回避）。PR ブランチ削除済み。addf-Behavior.toml の max_worktrees を 3→7 に変更（オーナー指示）。
**今の見立て**: オーナー確認待ち3件のうち「push 保留4コミット」が解消。残りは Issue #18/#19/#20 返信と v0.5.0 リリース判断。ローカル main は計画12＋設定1コミットが origin より先行（クリーンな fast-forward 関係）。
**次の自分へ**: 計画13コミットの push 方針は未指示のため保留のまま。次の実装タスクは 0035（合意済みの実装順: 0035→0036→0038→0037）。0028 フェーズ3-4 と 0033 項目4 は 0035 着手時に引き取り判断。
**気になっていること**: max_worktrees=7 で投機の同時本数が増えると、0035 の Pending 在庫上限5（持ち越し側）とのバランスが変わるかもしれない。0035 実装時に上限値の整合を一度見直すとよい。

##### 2026-07-05 — v0.5.0 リリース準備セッション（0028 3-4 実施中）
**やったこと**: オーナー合意「0028 3-4 を片付けて v0.5.0 → Issue 返信 → 0035〜0038 へ」。guides に投機運用ガイド新設（683d094）。README 和英に addf-speculate 追記（7874716 — v0.4.0 機能の掲載漏れを発見・修正）。overview full 再生成を委譲エージェントに実行中（0029 L1=GUI 常設前提の解消込み。途中停止したので再開指示済み）。未着手タスクのブロック判断済み: 0030〜0032/0035〜0038 はいずれも v0.5.0 をブロックしない。
**今の見立て**: 残りは overview 完了 → run-all＋lint → レビュー → Plan 0028 完了化・0029 L1 記録・TODO 更新 → release.exp ブラッシュアップ → Progress アーカイブ → コミット。リリース実行（タグ・push・GitHub Release）はオーナー確認後。
**次の自分へ**: release.exp に追記すべき「忘れがち」発見: (1) README 和英のスキル表・ガイド表への新機能反映漏れ（v0.4.0 の addf-speculate が漏れていた）(2) README ⇔ README.en は lint ペア化されていない手動同期。プレリリースチェックに「README 反映確認」を足す提案も ADDF-Release.addf.md に入れるとよい。
**気になっていること**: コンテキスト実測 187k。compaction が来たらこの日記とチェックリスト（TodoWrite ではなく本ファイル冒頭のサブタスクリスト）から再開する。overview エージェントの完了報告が来る前に compaction される可能性あり — その場合 .claude/addf/project-overview/ の git status で生成完了を判断する。

> 新しいタスク開始時は以下の構造で記録する:
> `### 現在のタスク: <Plan 名>` → `#### サブタスクチェックリスト` → `#### 日記`（運用ルール 3.5 の4項目書式）
